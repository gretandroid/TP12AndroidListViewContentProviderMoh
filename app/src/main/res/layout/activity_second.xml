<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SecondActivity">

    <ListView
        android:id="@+id/personListViewId"
        android:layout_width="0dp"
        android:layout_height="363dp"
        android:layout_marginTop="22dp"
        app:layout_constraintEnd_toEndOf="@+id/backButtonId"
        app:layout_constraintStart_toStartOf="@+id/backButtonId"
        app:layout_constraintTop_toBottomOf="@+id/backButtonId" />

    <Button
        android:id="@+id/backButtonId"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="22dp"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="22dp"
        android:onClick="onClickBackButtonEvent"
        android:text="BACK"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>